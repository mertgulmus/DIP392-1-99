import { PureComponent } from "react";
import './Play.style.scss';

export class Play extends PureComponent {
    render() {
        return (
        <div class="Play">
            <h1>Play</h1>
        </div>
        );
    }
}

export default Play;
